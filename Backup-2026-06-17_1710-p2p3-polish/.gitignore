.DS_Store
Alpha.zip
MAY 30 BU 2/
OG BACKUP/
.vercel
node_modules

# TTS generator Python venv (~750MB torch) + scratch — never commit. The generated
# audio under lessons-v2/audio/ is NOT ignored (it may be committed if desired).
tools/tts/.venv
tools/tts/_test.wav
tools/tts/_test.mp3
tools/tts/beats.json

# Generated by tools/gen-seo.js (rebuilt on every Vercel deploy)
/learn/
/sitemap.xml
/robots.txt

# Local-only Anthropic API key (never commit). Lives at the workspace root after the
# rebuild/ tree was removed; match the name anywhere.
.anthropic_key
