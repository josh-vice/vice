# PLAN (3) requirement matrix

This matrix tracks proof, not intent. A local test or build proves only the stated local boundary.

## ViceSuite integration baseline — `josh-vice/vice:viceterminal`

| Plan area | Current evidence | Status | Next proof or action |
| --- | --- | --- | --- |
| Canonical source convergence | `viceterminal` began as `646b2f8` with only `Start`. It now merges the exact ViceSuite baseline `af68d2e`; terminal, backend, scripts, and operational documentation are imported without generated dependencies or build output. | Statically and runtime verified | Complete deployment convergence and rollback drill while retaining the legacy deployment. |
| User-story expansion | US-001 through US-019 are checked in. US-013 has typed public market/timeframe context and canonical routes; US-016 now mirrors validated legacy Hub layout saves into versioned IndexedDB without changing legacy restore behavior. The remaining story acceptance and funded evidence are not complete. | Partial | Add each story's browser, migration, accessibility, visual, and funded evidence before promoting its status. |
| Widget evidence catalog | The shipped legacy Hub island has 58 mapped widget IDs, validated against `docs/widgets/vice-suite-widget-catalog.json`; `/hub`, `/scanner`, and `/gallery` serve that single runtime. Native `vWatch` and `vAlerts` ports are available at `/hub/native/watchlist`; they reuse the shared canonical public registry and browser-local alert monitor rather than a parallel feed or execution path. Native `vMovers` is at `/hub/native/movers`, ranking the shared exact registry rather than polling a second broad-market source. Native `vSuite` is at `/hub/native/suite`, preserving all legacy community links while adding direct Suite navigation. Native `vNotes` is at `/hub/native/notes` with one-time, device-local legacy scratchpad import; `vClocks` is at `/hub/native/session-clocks` with explicit time zones and regular-hours labels; `vCountdown` is at `/hub/native/candle-countdown` with UTC-aligned boundaries. Native `vPrice` is at `/hub/native/price`, reusing the canonical candle plane in an explicitly read-only chart mode that hides all private overlays and chart-order controls. | Locally verified; published to `viceterminal` on 2026-08-03 | Port the remaining widget contracts to native SvelteKit data/persistence surfaces and obtain per-widget acceptance evidence. |
| Insilico benchmark | `docs/competitive/insilico-matrix.json` is dated 2026-07-29, source-linked, schema-tested, and fails unsupported green claims. Its rows remain partial or behind. | Statically verified | Refresh official sources as needed and move a row to meets/exceeds only with direct measured evidence. |
| Unified SvelteKit shell | SvelteKit now serves `/`, `/hub`, `/trade`, `/scanner`, and `/gallery`. `/trade` is the sole terminal component; Hub sections preserve one vanilla widget runtime behind a same-origin public-context bridge that excludes account and signing state. Its Trade action carries a bounded timeframe plus exact Hyperliquid identity; Trade resolves only an exact catalog record and visibly fails closed when the settled catalog lacks it. | Statically and runtime verified | Replace the transitional Hub island incrementally with native components, preserve layouts, and add browser/mobile handoff proof. |

| Plan area | Current evidence | Status | Next proof or action |
| --- | --- | --- | --- |
| Truth baseline and fail-closed execution | On 2026-07-29, root `bun run test:release` passed preflight, launcher, source typecheck, zero-error Svelte source check, frontend and Rust tests, production build, security, contract, chaos, story, and release-policy suites. On the same date, root `bun run check` again passed preflight, source typecheck, zero-error Svelte check, 422 frontend tests, Rust tests, and the production build after surface assertions were moved from the Suite landing page to the single `TerminalWorkspace` execution surface and its `/trade` route. The isolated `bun run dev:verify` replay on `18580`/`15580` also passed backend health, public testnet session/book/feed checks, SSR and review-route policy, and explicit hosted-mutation rejection. Viceterminal now has an independent local Git boundary; the separate `vice-suite` repository and generated output are excluded. | Statically and runtime verified | Funded testnet and canary proof remain open. |
| Shared data plane and worker migration | All-mids, selected-market live trades, and selected `(coin, interval)` candles now run through an origin-wide SharedWorker with a dedicated-worker fallback, separate network epochs, exact tab fan-out, and worker-receipt-to-store timing. The worker strips trade user/hash fields and rejects invalid OHLCV values before the UI handoff. The UI-thread L2 snapshot boundary rejects malformed, unordered, and crossed books. Hyperliquid's current L2 payload identifies the coin but not the requested grouping, so books remain outside the worker; each terminal instance now uses a dedicated public L2 transport, separate from context subscriptions, to prevent ambiguous same-coin `nSigFigs` traffic from sharing that socket. On 2026-07-29, `dev:verify` passed public testnet all-mids, L2-book, and trades reachability after the split. | Statically and runtime verified | Obtain a grouping-aware subscription identity before shared-worker book multiplexing; obtain authoritative sequence support before claiming gap recovery; run Chromium burst/reconnect replay evidence. |
| Workspace and render surfaces | `WorkspaceHost` now owns a client-only Dockview desktop surface for the existing Chart, Markets, Depth & tape, Order ticket, and Account activity panels. Named local starting layouts use schema-versioned serialization and corruption fallback; presentation remains separate from market/account/execution stores. Schema-v2 rejects the prior malformed floating-layout shape, PANELS exposes an explicit local `Reset this layout` action, and floating/popout groups are not saved as a primary layout. The default layout sizes Markets at 280px, Depth & tape at 270px, Ticket at 320px, Account activity at 250px, and gives Chart the remaining width after the first measured frame. Panel visibility adds or removes only the selected panel, preserving the rest of a trader's dock arrangement. Inactive panels use Dockview's `onlyWhenVisible` renderer. Each tab has presentation-only `Float panel`, `Pop out panel`, standalone snapshot, and explicit cyan/amber/violet public-link snapshot actions. Snapshot instances retain an exact `marketKey`; link contexts persist only exact market/timeframe values; both read only public registry quote/context fields and can explicitly select the shared market. They have no execution, account, book, or chart context. Floating groups stay bounded to the terminal viewport and use a separate drag titlebar, while popouts use a static same-origin no-script shell and move the existing panel container. Dockview keyboard navigation is enabled. The lazy Dockview client chunk is statically capped at 80 KiB gzip (current build: 73,959 bytes). Dither states, local privacy masking, CLI chains plus bounded repeat with local aliases/variables, panel controls, configurable buy/sell/size/CLI/timeframe/click-placement/panel-focus hotkeys, and exact-identity market/quote/Scale/TWAP close, reverse, and serial multi-market flatten remain. On 2026-07-29, a live public browser reset measured a 280/410/270/320px top row plus a 250px activity row at 1280px, with wallet disconnected and trading disabled. | Statically and browser verified | Verify drag/redock/tab/popout; add independently fed chart/book/ticket contexts, crosshair/account instances, hide/show frame-memory measurements, and certified flatten/reverse bindings. |
| Direct and persistent execution | Local signing, journal, reconciliation, and local tab-bound algorithms exist. Every local place, cancel, modify, Scale, and TWAP signer call requires the matching canonical Hyperliquid instrument, so an API coin, asset/product, terms, or precision mismatch fails before a venue payload is built. Account-local fat-finger caps use fixed-point values at the shared pre-signing boundary and fail closed on partial position identity. Confirmed reverse closes first and opens only after an exact flat account snapshot. Confirmed cancel-all reports an order as cancelled only after the refreshed open-order snapshot omits it. Versioned direct Scale, conditional ladders, Chase, trailing, Iceberg, Ping-Pong, adaptive/VWAP, POV, and break-even jobs persist exact pre-dispatch IDs and recover only complete journaled child IDs. Versioned OCO and Swarm journal every sequential leg, adopt only proven children, and pause incomplete work until a user explicitly resumes on the exact market. A live non-private account may explicitly download a local schema-v1 audit record with identity, command IDs, sequence, cloids, exact venue IDs, status, and timestamps; it excludes keys, signatures, raw request bodies, and raw errors and never uploads. Older records pause after restart until they gain that recovery proof. | Statically verified | Funded lifecycle proof; full browser restart evidence; hosted executor authority remains unimplemented. |
| Automation persistence disclosure | The ticket labels venue-native orders and device-local persisted algorithms before submission; local jobs explicitly state that Vice servers do not run them. A shared fail-closed trigger engine models price, candle-close, candle-volume, time, and synthetic-pair semantics. Conditional ladders can use price-cross, completed candle close/volume for the selected exact market and saved interval, a future local time, or ratio/spread versus one distinct exact reference market. A separate candle-health signal prevents trade-derived chart fallback from arming or firing candle triggers. Pair triggers use fresh exact all-mids values for both legs; time triggers never backfill after restart or a clock outage. All sources still require live selected-market data before dispatch. The fired edge and each pending conditional-ladder or direct-Scale batch command ID persist before dispatch; Chase and versioned trailing or Iceberg jobs do the same before each child placement, while versioned OCO journals each sequential leg. On restart, a complete journaled result restores exact child IDs; a missing or unresolved dispatch pauses rather than resending, and incomplete OCO pairs require an explicit exact-market resume. Browser-local telemetry records only persistence class, runtime, trigger type, and fire/pause/miss counts; no strategy parameters or account data enter it. The local testnet browser loaded live public data and displayed the telemetry panel without console errors; no wallet or execution action was used. The user-run trade-only runner engine (`src/lib/runner/`) now fails closed on kill switch, missing consent/focus, revoked or expired pairing, stale feeds, expired commands, and non-running states; uncertain outcomes pause the runner instead of replaying; jobs dispatch only through the shared certified action boundary. Pairing and cross-device envelope contracts validate format, expiries, revocation epochs, and reject any plaintext secret field; the module never signs, holds credentials, or opens a transport. | Statically verified | Obtain trigger-lifecycle browser and funded evidence; wire the runner into the certified action boundary and a user-facing start/stop surface. |
| Multi-account aggregation and basket execution | `src/lib/venue/aggregation.ts` provides read-only account aggregation that preserves venue, environment, account reference, instrument key, source timestamp, and exact health for every row; orders rows deterministically; applies per-venue health with strict venue isolation so a degraded venue can never mark another venue stale; and rejects cross-venue inference before any payload attribution. Anonymous venue-health telemetry counts only. `src/lib/execution/basket.ts` plans baskets by previewing every leg against exact account/instrument identity and certified venue capabilities (an uncertified venue or unsupported order type fails the preview), orders legs deterministically so identical baskets produce identical plans and child command ids, journals every deterministic child command before dispatching any leg in a versioned device-local journal, stops dispatching on the first uncertain outcome, reconciles every involved venue before reporting completion, and reports `complete` only when every leg is reconciled, `partial` otherwise with explicit paused-leg reasons. The basket module creates no transport, signer, credential, or execution path of its own; dispatch and reconciliation are injected through the shared certified boundary. | Statically verified | Wire aggregation and basket preview into a user surface; obtain funded per-leg place/partial-fill/cancel/reconnect/restart/revoked-key and uncertainty evidence before exposure. |
| Venue waves | Hyperliquid is the only exposed venue. Canonical venue identity now requires an exact venue, instrument key/symbol, product, base/quote/settlement assets, decimal contract multiplier, price rule, and size increment; event envelopes require a venue, epoch, receipt time, dedupe key, and keys scoped to that venue. Hyperliquid core perp, HIP-3, spot, cached, and bootstrap descriptors map to it; their price rule preserves the official five-significant-figure and size-dependent decimal constraint instead of claiming a fixed tick. Its accepted WebSocket books and HTTP L2 baselines enter through that canonical envelope; a late HTTP baseline cannot replace an accepted live frame. Because the venue supplies no book sequence, the envelope uses a connection/subscription epoch and local ordinal rather than fabricating `venueSequence`. Outcome metadata remains unroutable until it supplies complete execution terms. Lighter now has a public review-only catalog boundary: complete spot pairs map to canonical identities, while perpetual records remain metadata-only because the official response omits a settlement-asset symbol. On 2026-07-29, the exact adapter read 209 active mainnet records (7 canonical spots; 202 blocked perpetuals), with no credential or mutation. Its new exact-spot public-book boundary waits for the observed application handshake, uses only Lighter's read-only endpoint, applies only nonce-contiguous updates, and reconnects from a new snapshot; one credential-free ETH/USDC snapshot was read live. It remains absent from navigation and has no signer, account, or order route. BloFin maps its exact contract records into that shared identity while retaining its original `instId`; its declared venue capabilities are explicitly `reviewOnly`, not a Vice trading certification. Its public-book adapter now requires that exact catalog identity and emits only sequence-safe canonical book events with connection epoch, venue sequence, receipt time, and a dedupe key. Its private snapshots carry a validated, non-secret `AccountRef` made from the selected environment and local credential fingerprint, never a claimed venue account ID. BloFin also has exact public catalog data, a sequence-aware public-book boundary, a local AES-GCM credential vault, a review-only local credential setup route, signed all-or-nothing private balance/position/open-order reads, and fixed-input tests. The setup review can explicitly unlock one locally stored fingerprint and perform one browser-direct private HTTPS verification; it clears the phrase and returns only time and record counts, not account values. Its private-stream client signs a local WebSocket login, subscribes only after acknowledgement, coalesces account/order/position/algo-order signals into all-or-nothing REST reconciliation, and reconnects after a missed heartbeat without exposing raw private frames. It has no automatic unlock, connection, navigation, or order path. The local vault now indexes only environment-scoped key fingerprints, so a trader can list and explicitly remove device-local ciphertext; removal never claims to revoke a venue key. Its public catalog has a separate review-only page, absent in production unless `VITE_BLOFIN_PUBLIC_REVIEW=true`; its browser client uses a same-origin no-store public route with an 8-second timeout and the existing strict public parser. On 2026-07-28, that review page rendered 88 validated demo contracts and filtered exact `BTC-USDT` to one row with no wallet, credential, account, order, or transfer control. Its public-feed adapter rejects malformed/gapped frames, marks the feed stale, and reconnects for a fresh snapshot in contract tests. On 2026-07-28, an unauthenticated demo two-connection probe observed fresh `BTC-USDT` 400-bid/400-ask snapshots followed by contiguous updates: `33227562719`/`33227563119`, then `33227563529`/`33227563729`; the client reported a clean reconnect. No credentials or mutations were used. No private live evidence or order path exists. | Live-read verified | Obtain authoritative Lighter perpetual settlement terms and browser idle/reconnect proof before any Lighter routing; migrate remaining new-venue adapters to the canonical contract; prove deliberate Hyperliquid stale recovery, connected private reconciliation and reconnect, venue-side key rotation/revocation, and funded-demo order evidence before exposure. |
| Nado public discovery | The official V2 archive `contracts` response gives exact product IDs, tickers, base/quote strings, type, and mark prices. `nado/public.ts` preserves those 72 currently priced mainnet perpetual records, but marks every one metadata-only: the source has no fixed price/size increment or contract multiplier. V2 pairs advertises spot pairs through another endpoint; Vice does not merge it with archive data or infer missing terms. `nado/publicWs.ts` now preserves the documented x18 book-depth protocol and timestamp-gap boundary behind a required authoritative snapshot, without float conversion, an instrument, a signer, or terminal exposure. A local Bun WebSocket attempt did not connect, so no live Nado-stream claim exists. No credential, wallet, order, or navigation path exists. | Live-read catalog; static stream protocol | Obtain one authoritative source that gives complete product precision and multiplier terms plus a verified MarketLiquidity snapshot integration and browser/standards-client stream evidence; then build a complete public feed before any signer, account, or terminal exposure. |
| Hyperliquid product classes | Core perp, HIP-3, spot, and official prediction-outcome identities exist. The catalog also preserves exact official perp categories and the watchlist searches/groups core and HIP-3 rows by those descriptive categories without inferring tokenization, RWA status, or routing terms. Spot identity requires non-empty trimmed base/quote token-map values and a bounded integer venue size precision; a malformed spot is skipped rather than inferred from a display label, assigned a default quote, or assigned a synthetic whole-unit size increment. On 2026-07-29, the isolated public browser logged one skipped malformed spot, then retained `DATA LIVE` with 157 core rows and 1,778 markets; catalog status was correctly degraded because separate HIP-3 enrichment hit a rate limit. Outcome rows carry official question/side metadata and canonical asset IDs, but the current venue response omits complete lot, tick, and execution terms, so trading is explicitly blocked. Broad RWA classification remains absent. | Statically and browser verified | Obtain authoritative outcome execution constraints and RWA classification; then certify feed, account, sizing, and funded lifecycle paths. |
| Market-data richness | Stats now use a separately monitored context feed and hourly funding timer; price alerts monitor every configured exact market from the existing all-mids registry; the DOM shows a bounded top-12 displayed-size imbalance; local sound controls, privacy masking, tape, grouping, stat-timing, identity, catalog, full frontend, typecheck, and build checks pass. The isolated-port runtime verifier also passed testnet all-mids/book/trades smoke. | Statically verified | The official public Hyperliquid trade schema has no liquidation marker, so the tape names liquidation data unavailable instead of fabricating labels. Browser interaction and deliberate reconnect/stale-transition proof remain open. |
| DOM ladder click trading | Live book rows now provide armed exact-price limit and Shift-click stop entry, authoritative row-aligned orders and positions, pending-safe individual and side cancel, drag-reprice, shared quick-size presets, selected-market flatten/reverse, a manual/auto recenter control, and price-keyed rows through shared local execution boundaries. | Statically verified | Obtain browser interaction and measured frame-level row-diff evidence, then funded lifecycle proof. |
| Release and performance gates | Static release gates passed locally on 2026-07-29; the isolated runtime verifier also passed public testnet session/book/all-mids/L2/trades, SSR policy, and cleanup. Downloadable schema-v2 client evidence includes receipt/store/frame-ready samples plus privacy-safe long-task, long-animation-frame, event-delay, inferred dropped-frame, queue-depth, reconnect, and optional heap aggregates. No measured desktop capture is certified. | Statically and runtime verified | Measured Chromium, venue, funded, and canary evidence. |

## External gates and authority

| Gate | Current state | Blocker | Next action |
| --- | --- | --- | --- |
| Interactive browser DOM/chart proof | On 2026-07-28, the in-app browser loaded the local terminal with live public BTC data and 1,465-market catalog results; selected Chart max then Default, reloaded with Default restored, recentered the DOM ladder, changed book grouping to 4 significant figures and depth to 24 levels, and reloaded with both selections restored. Console warnings/errors were empty. The wallet remained disconnected and every trading action stayed disabled. | Live-read verified | Run the existing browser interaction suite; retain screenshots/traces for chart placement, DOM drag/cancel, floating/popout, stale/reconnect, and mobile flows. |
| Funded Hyperliquid testnet lifecycle | Not attempted | It would submit venue orders and requires a funded testnet account plus explicit approval | Provide the funded testnet account and authorize low-notional place/modify/cancel/partial-fill/reconnect/restart evidence |
| Low-notional canary and mainnet promotion | Not attempted | Requires separate release authority; mainnet remains fail-closed | Approve an allowlist, cap, observation window, and named operator only after funded certification passes |
| Additional venues | BloFin public catalog foundation is local-only and unexposed; no certified adapter beyond Hyperliquid | BloFin demo API credentials, official private fixtures, and per-venue certification are absent | Create a BloFin demo key with READ + TRADE only (never TRANSFER), then run key-rotation, revoked-key, and funded-demo lifecycle evidence before enabling navigation |
| Hosted persistent executor | Intentionally unimplemented | It needs a separate product/security decision, revocable authority design, KMS/Postgres/lease infrastructure, and explicit user opt-in | Approve the hosted-authority scope and operating environment; otherwise retain the documented browser-local and venue-native persistence classes |
