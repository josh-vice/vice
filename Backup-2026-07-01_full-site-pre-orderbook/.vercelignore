tools/chart-audit
# Discord banner build tooling + assets (not part of the website)
discord-banners

# Internal docs / agent files — never deploy publicly
AGENTS.md
BUILD-PLAN.md
HEARTBEAT.md
IDENTITY.md
SOUL.md
TOOLS.md
USER.md
course1-notes.md
course2-notes.md
course3-notes.md
course4-notes.md
HANDOFF.md
OPENCLAW-HANDOFF.md

# Legacy / prototype pages superseded by index.html + lt-index.html
liquidity-theory-course1.html
sim-test.html
trading-demo.html
trading-quiz.html

# Local source-snapshot backups — internal restore points, never serve publicly.
Backup
Backup-*

# Stray dir from a mis-expanded ~ path (internal transcripts) — never deploy.
~

# Build/source artifacts not needed at runtime
og-card.svg        # source for og-image.png (the exported PNG is what's served)
serve.json         # local `npx serve` config only; Vercel uses vercel.json

# TTS narration generator (Kokoro). The Python venv (~750MB torch) + scratch files
# never deploy; the GENERATED audio under lessons-v2/audio/ DOES ship. (tools/gen-seo.js
# still deploys — it runs at Vercel build — so we exclude only the tts venv + scratch.)
tools/tts/.venv
tools/tts/beats.json
tools/tts/_test.wav
tools/tts/_test.mp3

# The rebuild/ tree (v1 reconstruction recipes/audio/frames/generators + the older
# rebuilt-transcript layer) was DELETED — superseded by the v2 animated lessons. The
# recon player (lt-recon-player.js) was removed too. Guard against any stray reappearance:
rebuild
player

# v2 animated-lessons system (lessons-v2/): SHIP the runtime — renderer.js,
# chart-vocabulary.js, player.css, and lessons/**/*.js + manifest.js (the live app
# loads these). Exclude only the internal docs + the standalone preview hubs.
lessons-v2/*.md
lessons-v2/course1.html
lessons-v2/course2.html
lessons-v2/course3.html
lessons-v2/course4.html
lessons-v2/preview.html

# SECURITY: the Anthropic API key must never be deployed (kept at the workspace root
# after the rebuild/ tree was removed). Match it anywhere, just in case.
.anthropic_key

# Internal reports + visual-audit artifacts (OpenClaw audit work — never serve publicly)
audit.md
sweep_report.md
audit_frames
f_*.jpg
og-render

# Design-context docs + agent-skill tooling (impeccable / emil-design-eng) — internal, never serve
PRODUCT.md
DESIGN.md
.agents
.impeccable

# OS / tooling junk
.DS_Store
.claude
CHART_AUDIT_HANDOFF.md
UI_POLISH_HANDOFF.md
BRAND_KIT.md
