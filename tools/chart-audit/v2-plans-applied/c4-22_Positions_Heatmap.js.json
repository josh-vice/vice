{
  "course": "4",
  "file": "22_Positions_Heatmap.js",
  "lessonId": "course4/22_positions_heatmap",
  "edits": [
    {
      "beatId": "reading",
      "beatType": "CHART",
      "move": "liquidation_levels",
      "addShow": [
        { "kind": "marker", "at": "reversal", "label": "sweep & reverse", "style": "reversal", "place": "above" }
      ],
      "reason": "Say teaches a distinct reversal event after the sweep: \"Watch price get drawn straight up into the hot band, sweep it, and reverse.\" The existing marker at 'magnet' covers price being drawn up into / sweeping the hot band, but the reverse is a separate locatable event. 'reversal' is a valid point anchor for liquidation_levels and is currently undrawn; marker-on-point type matches. place 'above' keeps the label clear at the top of the upward draw."
    }
  ],
  "alreadyGood": ["intro", "use", "recap"],
  "notes": "Move liquidation_levels anchors: clusterHi(L), clusterLo(L), magnet(pt), reversal(pt). Auditor's single gap is accepted as-is (label normalized to 'sweep & reverse' to match the say's 'sweep it, and reverse'). Existing shows verified correct: hot-band zone above clusterHi, magnet level at clusterHi, lighter-band zone below clusterLo, and the price-seeks-magnet sweep marker at magnet — all non-redundant and anchor-valid. No anchor misses found. CONCEPT beats (intro/use/recap) draw nothing and are skipped. No noAnchorGaps."
}
