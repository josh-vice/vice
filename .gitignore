.DS_Store
Alpha.zip
MAY 30 BU 2/
OG BACKUP/
.vercel
node_modules

# TTS generator Python venv (~750MB torch) + scratch — never commit. The generated
# audio under lessons-v2/audio/ is NOT ignored (it may be committed if desired).
tools/tts/.venv
tools/tts/_test.wav
tools/tts/_test.mp3
tools/tts/beats.json

# Generated by tools/gen-seo.js (rebuilt on every Vercel deploy)
/learn/
/sitemap.xml
/robots.txt

# Local-only Anthropic API key (never commit). Lives at the workspace root after the
# rebuild/ tree was removed; match the name anywhere.
.anthropic_key

# iOS app (IOSApp/): the native Xcode project IS tracked; generated bundle,
# deps, and pods are rebuilt locally (www/ regenerates on every commit via hook)
IOSApp/node_modules/
IOSApp/www/
IOSApp/ios/App/Pods/
IOSApp/ios/DerivedData/

# ── OpenClaw assistant runtime + private config (local machine only — the
#    agent needs these on disk; they are not part of the site) ──
AGENTS.md
HEARTBEAT.md
IDENTITY.md
MEMORY.md
PRODUCT.md
SOUL.md
TOOLS.md
USER.md
openclaw-workspace-state.json
/memory/
.agents/
.clawhub/
.codex/
.openclaw/
.claude/
.anthropic_key

# session archives live outside the repo (…/.openclaw/workspace-archive)
Backup-*/
