# SESSION HANDOFF — 2026-07-03 (mega-session: TTS, terminal sim, Geist, app-feel)

> Read `CLAUDE.md` first (ground rules — updated this session: **app entry is now `index.html`**,
> final exam REMOVED, font is **Geist Mono**). Prior context: `HANDOFF_2026-07-02_review-round2.md`.
> Memory files updated: `lt-simulator-terminal`, `lt-mobile-ios-layer`, `lt-tts-elevenlabs`,
> `lt-final-exam` (removed), `lt-labs` (8 labs), `liquidity-theory-project`.

Serve: `python3 -m http.server 5313 --directory .` → open `/`. Preview MCP config `lt-tts-eleven`
(port **5314**) in `/Users/pbot/.claude/launch.json`. Deep-link `?c=<1-4>&ch=<0-based>&s=<step>` after
`ltSetUnlockAll(true)`. **NOTHING IS DEPLOYED** — owner deploys manually after review. Everything below
is verified live (0 console errors at each step).

---

## ⚠️ OPEN TODOs — ✅ ALL THREE FIXED 2026-07-04 (see HANDOFF_2026-07-04.md; awaiting owner review)

1. **Header spacing "off" (mobile nav bar)** — owner screenshot: the `Module 1 · Session 1` /
   `Understanding Pr…` block + Discord/glossary/settings icons feel unevenly spaced at ~390-430px.
   Audit `.top-header` children gaps/paddings at phone widths (`lt-styles.css` base + ≤768 block).
   Related unresolved nit from earlier: the Discord icon box *measures* centered (15/15 pad) but owner
   perceives more room above than below — likely optical (glyph shape); consider a 0.5–1px optical
   nudge of the svg, and compare `.header-discord` box (34×34 at laptop widths) vs neighbouring
   `.icon-btn` (36×34) — 2px mismatch, unify.

2. **Marquee the truncated header title** — owner wants the ellipsized chapter title
   (`#header-title`, e.g. "Understanding Pr…") to slowly auto-scroll horizontally like a stock ticker
   so the full title is readable, then loop/reset. SLOW, subtle. Implement CSS keyframes on overflow
   (translateX of an inner span, pause at each end), only when actually overflowing (JS check:
   `scrollWidth > clientWidth`, add a class), respect `prefers-reduced-motion` (static ellipsis).
   It's the only text in the app that's ever cut off — that's why he wants it.

3. **v2 lesson canvas labels clip on mobile** — e.g. C1 ch0 lesson beat 3: annotation
   "body — open to close · green = clo…" runs off the right edge of the canvas. **Pre-existing**
   (visible in owner's own older screenshots), NOT caused by the Geist swap (measured: Geist and
   JetBrains Mono are metrically identical, ratio 1.000) and NOT by the uniform-player work (stage is
   back at the calibrated 4:3, aspect verified 1.33). Root cause: authored label x-anchors + fixed-px
   canvas fonts leave too little room at phone widths, and the renderer's ellipsis fitter
   (`lessons-v2/renderer.js` ~line 947, "hard ellipsis only as a last resort") is NOT applied on the
   callout/annotation label path (~lines 1098/1151/1190+). Fix = wire the fitter (or a shift-left
   clamp) into those label draws, then run the full v2 render sweep (ground rules: every
   `LT_LESSONS[id]` CHART/CANDLE beat through `new LTRenderer(...)` → 0 throws) + eyeball several
   lessons at ≤400px. This touches all 83 lessons' rendering — its own careful task.

---

## What shipped this session (chronological, all verified + tokens bumped)

1. **ElevenLabs TTS migration** — all 84 lessons/525 clips regenerated (`eleven_flash_v2_5`, voice
   `l30f87tf05uxyknGdDw6`); `tools/tts/generate-eleven.mjs` (resumable; per-lesson regen); word-level
   karaoke timings preserved. `audioVersion 3.0.0`. Creds `/Users/pbot/.openclaw/lt-tts.env` —
   **owner should rotate the API key** (was pasted in chat). Kokoro `generate.py` kept as fallback.
2. **Completion >100% bug fixed** — stale localStorage chapter indices counted unbounded;
   `_ltCountCompleted(prog, chapterCount)` in lt-engine clamps to current chapters.
3. **Flashcards** — top-anchored stage (was vertically centred → sank); grid-stacked flip faces
   (card auto-grows; mobile overflow fixed).
4. **Landing page work then FULL REMOVAL** — the marketing landing is GONE; **the app IS the site
   root** (`index.html`); old `lt-index.html` = redirect shim preserving query+hash (KEEP IT — old
   bookmarks + `#sync=` links). SEO/OG head carried onto the app. `learn/` links → `/`.
5. **Final exam + certificate REMOVED sitewide** (owner order) — engine block, screens, settings
   answer key, data pools (~1,800 lines, 10 files). `getCourseName()` survived (flashcards uses it).
   Do not resurrect.
6. **Mobile iOS layer** (`lt-styles.css` end, ≤768) — rounded radius tokens (12/16/8), safe-areas
   (`viewport-fit=cover`), bottom-sheet modals w/ grabber, universal press states. Owner REJECTED a
   bottom tab bar (built then removed — frequency-of-use test; don't re-propose).
7. **Practice Simulator v3 "Terminal"** — ground-up rebuild (`lt-sim-ui.js` v2, `sim3-` prefix):
   live tick replay (play/pause, 1×–10×), market/limit/stop orders, one-way netting, isolated margin,
   taker 0.055%/maker 0.02%, slippage, proper liq (margin wiped), draggable SL/TP, sessions +
   pattern-reveal verdict. Stats schema v2 (`lt_sim_stats`, v1 auto-migrates). Stress-tested:
   481k candles, 4k accounting walks (drift ~1e-12), soak, fuzz ($10 min order added). Engine API
   unchanged (`renderSimulator/ltSimTeardown/getSimPatternForChapter`).
8. **Community page** — flagship rebuild (link-rows, promise 2-col, quiet CTAs, no brand slabs).
9. **Premium detail layer** (`lt-styles.css`) — ambient card shadows, shell depth, desktop press
   states, frosted toasts, search focus ring, 3D pointer tilt on home/team cards (`_ltInitCardTilt`).
10. **Spacing rhythm normalized** — course view 18px (untouched); module pages 14px blocks / 12px
    grids; odd paddings evened. Roadmap-card gap-0 collision fixed (+ site-wide probe = clean).
11. **Glossary** — 24 figures rebuilt as candle-drawn mini-charts (`_gsvg2` 210×132 canvas, `_run`
    helper); `_ichiBase` full redraw (thick two-tone Kumo, twist, projection, spotlight API kept);
    figures NEVER name their term (flashcard rule — verified). **References curated**: relevance bar
    (title-match OR ≥3 mentions OR pinned; recap/outro banned; cap 4) → 825→259 refs; pins added for
    14 orphan terms (careful: C2 app chapter indices ≠ audio numbering — no intro chapter).
12. **Settings** — de-iconed title, sentence-case labels, danger→pink, quiet hover-reveal buttons.
13. **Chrome (restraint pass)** — `/` + ⌘K focus search (+ kbd chip), tick-down flash → pink,
    sidebar scroll-edge fade masks. Rejected: everything else (already good).
14. **Chart reveals silked** — both quiz + teach paths: rAF cubic ease-in-out timeline (replaces
    190ms metronome), overlapping 260ms candle grow-ins, 160ms landing beat, reduced-motion instant.
15. **Geist Mono sitewide** — 224 refs across 23 files incl. ECharts fonts + v2 canvas fonts +
    gen-seo (Barlow purged). Google Fonts: `Geist+Mono 400–800`. `tools/build-og.js` untouched
    (owner kept old OG card — proposed redesign REJECTED, scratchpad only).
16. **Candle Types lab** (8th lab) — gallery converted from intro-chart-slot hack to proper
    `demo:{kind:'gallery'}` step on C1 ch0 (`after:'intro'`); engine special case deleted; ch0's real
    introChart restored. Then lab REBUILT: click/tap+arrow selection (ARIA tabs), 144px hero preview,
    palette chips, prev/next + counter, proper containerId mount. Gotcha fixed: arrow keys needed
    `stopPropagation` (engine pages steps on arrows at document level).
17. **Lesson player** — caption beat-type tag REMOVED entirely (owner: waste of space; kinds still
    drive stage behaviour); mobile transport buttons real flex (glyphs actually centred); progress
    row packs (min-width 4px segs) — fits ≥330px; **app-fit**: lesson step scroll slack = 0 (trimmed
    content-area 12/8, banner 10, watch-link slim, `last-child` margin kill) — scroll ONLY when
    content truly overflows; footer 54→62 (even 7px button air); **uniform player**: caption =
    fixed-height block w/ internal scroll + karaoke `scrollIntoView nearest`, concept beats share the
    fixed stage (4:3 mobile / 16:9 desktop, `overflow-y:auto` valve) → player height + button
    positions IDENTICAL on every beat (verified 519px/Y605 mobile × 5 kinds, 703px desktop).
    **Do NOT flex the stage** — tried, it breaks chart annotation geometry (reverted).

## Cache tokens as shipped (in `index.html`)
styles **1.112.5** · engine **3.151.2** · data **1.18.2** · data-c2 **1.22.0** · data-c3 **1.20.0** ·
data-c4 **1.27.0** · settings **1.37.1** · sim-ui **2.0.3** · simulator **1.4.0** · community
**2.0.2** · glossary **2.1.3** · flashcards **1.9.10** · gallery **2.0.1** · store **1.0.1** ·
player.css **1.11.5** · renderer **1.23.3** · chart-vocab **1.14.0** (unchanged) · v2 lesson token
**1.0.9** (unchanged) · audio manifest **2.0.0** → **audioVersion '3.0.0'** in engine.
(Labs all bumped +.0.1 during the Geist swap.)

## Gotchas that WILL bite
- **App entry = `index.html`**; `lt-index.html` is only a redirect shim — keep it.
- Cache-bust in `index.html` now (ground rules updated).
- Final exam/certificate: removed — don't reference in new copy. `lt_examDone_*` keys are dead.
- Sim money model: margin is a RESERVATION (never moved through balance except liq deduction) —
  don't "release" margin into balance on close (that phantom-credit bug was fixed).
- Glossary figure rule: figures must never name their own term (Definition→Term flashcards).
- Glossary ref pins use APP chapter indices (C2/C4 have no intro chapter; C3 DOES at idx0).
- Owner rejected: bottom tab bar, OG redesign, absolute-centred header title (collides with icons).
- Stage aspect = calibrated chart geometry: never flex/auto it on chart/candle beats.
- Backups: every change-set has `Backup-2026-07-0{2,3}_<slug>/` at repo root; full pre-overhaul
  snapshot `Backup-2026-07-02_pre-mobile-overhaul/` (190MB).
